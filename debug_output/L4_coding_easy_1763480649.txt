```json
{
  "title": "Inventory Threshold Alert",
  "description": "You are managing an inventory system for an e-commerce platform. You have a list of item quantities and a critical threshold. Your task is to identify and return a list of item IDs that fall below the specified threshold. Each item has an ID represented by its index in the quantity list (starting from 0).",
  "input_format": "The input consists of two parts:\n1.  An array of integers `quantities` representing the current quantity of each item in the inventory.\n2.  An integer `threshold` representing the minimum acceptable quantity. If an item's quantity is strictly less than the threshold, it triggers an alert.",
  "output_format": "Return a list of integers representing the item IDs (indices) that are below the `threshold`. The item IDs should be sorted in ascending order.",
  "constraints": "1 <= `quantities.length` <= 100\n0 <= `quantities[i]` <= 100\n0 <= `threshold` <= 100",
  "sample_input": "[5, 2, 8, 1, 9, 0]\n3",
  "sample_output": "[1, 3, 5]",
  "difficulty": "easy"
}
```