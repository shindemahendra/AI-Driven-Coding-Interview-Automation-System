```json
{
  "title": "Optimal Resource Allocation for Microservice Deployments",
  "description": "You are building a deployment pipeline for a suite of microservices. Each microservice has a specific resource requirement (CPU, memory) and a value representing its importance to the overall system. You have a limited amount of total resources available to allocate across multiple deployment environments (e.g., staging, production). Your goal is to determine the optimal allocation of microservices to environments to maximize the total value of deployed microservices, subject to the resource constraints of each environment and the availability of each microservice.\n\nYou are given the following:\n\n*   `services`: A list of tuples, where each tuple represents a microservice: `(name, cpu_requirement, memory_requirement, value)`. `name` is a string, `cpu_requirement` and `memory_requirement` are integers representing the resources needed, and `value` is an integer representing the importance of the service.\n*   `environments`: A list of tuples, where each tuple represents a deployment environment: `(name, cpu_capacity, memory_capacity)`. `name` is a string, and `cpu_capacity` and `memory_capacity` are integers representing the resource limits of the environment.\n*   A service can be deployed to at most one environment.\n\nYour task is to return a dictionary representing the optimal deployment plan. The keys of the dictionary are the environment names, and the values are lists of service names deployed to that environment. The deployment plan should maximize the total value of deployed microservices without exceeding the resource capacities of any environment.\n\nIf a service cannot be deployed in any environment due to resource constraints, it should not be included in the deployment plan. If multiple optimal solutions exist, you can return any one of them.",
  "input_format": "The input will consist of two lists of tuples:\n\n*   `services`: A list of tuples, where each tuple is `(service_name: str, cpu: int, memory: int, value: int)`\n*   `environments`: A list of tuples, where each tuple is `(environment_name: str, cpu_capacity: int, memory_capacity: int)`",
  "output_format": "A dictionary where keys are environment names (strings) and values are lists of service names (strings) deployed to that environment. Example: `{\"staging\": [\"serviceA\", \"serviceB\"], \"production\": [\"serviceC\"]}`",
  "constraints": "*   1 <= number of services <= 10\n*   1 <= number of environments <= 5\n*   1 <= cpu_requirement, memory_requirement, value <= 100\n*   1 <= cpu_capacity, memory_capacity <= 500\n*   Service names are unique.\n*   Environment names are unique.",
  "sample_input": "services = [\n    (\"serviceA\", 20, 50, 60),\n    (\"serviceB\", 30, 40, 70),\n    (\"serviceC\", 50, 60, 80),\n    (\"serviceD\", 10, 20, 30)\n]\nenvironments = [\n    (\"staging\", 100, 150),\n    (\"production\", 80, 100)\n]",
  "sample_output": "{\n    \"staging\": [\"serviceA\", \"serviceB\", \"serviceD\"],\n    \"production\": [\"serviceC\"]\n}",
  "difficulty": "hard"
}
```