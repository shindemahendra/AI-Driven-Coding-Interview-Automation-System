```json
{
  "title": "Optimal Resource Allocation for Cloud Instances",
  "description": "A cloud provider offers various types of virtual machine (VM) instances, each characterized by its CPU cores, memory (in GB), and cost per hour. A client needs to run a set of applications, each with specific CPU and memory requirements. The goal is to minimize the total cost of running all applications by optimally allocating them to the available VM instances. Note that a single VM instance can host multiple applications as long as its CPU and memory capacity is not exceeded.\n\nYou are given:\n\n*   `instances`: A list of dictionaries, where each dictionary represents a VM instance and has the following keys:\n    *   `cpu_cores`: Integer representing the number of CPU cores.\n    *   `memory_gb`: Integer representing the memory in GB.\n    *   `cost_per_hour`: Float representing the cost of the instance per hour.\n*   `applications`: A list of dictionaries, where each dictionary represents an application and has the following keys:\n    *   `cpu_cores_required`: Integer representing the CPU cores required.\n    *   `memory_gb_required`: Integer representing the memory in GB required.\n\nAssume that an application cannot be split across multiple VM instances. Your task is to write a function that determines the minimum total cost per hour to run all applications.\n\nIf it is impossible to run all the applications with the given instances, return -1.",
  "input_format": "The input consists of two lists of dictionaries:\n\n*   `instances`: A list where each element is a dictionary with keys `cpu_cores`, `memory_gb`, and `cost_per_hour`.\n*   `applications`: A list where each element is a dictionary with keys `cpu_cores_required` and `memory_gb_required`.",
  "output_format": "Return a float representing the minimum total cost per hour to run all applications. Return -1 if it is impossible to run all applications.",
  "constraints": "*   1 <= number of instances <= 10\n*   1 <= number of applications <= 15\n*   1 <= cpu_cores, cpu_cores_required <= 32\n*   1 <= memory_gb, memory_gb_required <= 64\n*   0.1 <= cost_per_hour <= 100.0\n*   All input values are non-negative.",
  "sample_input": "{\n  \"instances\": [\n    {\"cpu_cores\": 2, \"memory_gb\": 4, \"cost_per_hour\": 1.0},\n    {\"cpu_cores\": 4, \"memory_gb\": 8, \"cost_per_hour\": 2.0},\n    {\"cpu_cores\": 1, \"memory_gb\": 2, \"cost_per_hour\": 0.5}\n  ],\n  \"applications\": [\n    {\"cpu_cores_required\": 1, \"memory_gb_required\": 2},\n    {\"cpu_cores_required\": 2, \"memory_gb_required\": 4},\n    {\"cpu_cores_required\": 1, \"memory_gb_required\": 1},\n    {\"cpu_cores_required\": 1, \"memory_gb_required\": 2}\n  ]\n}",
  "sample_output": "2.5",
  "difficulty": "hard"
}
```