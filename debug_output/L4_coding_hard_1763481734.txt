```json
{
  "title": "Optimal Resource Allocation in a Distributed System",
  "description": "You are managing a distributed system composed of 'n' nodes, each with a limited processing capacity. You have 'm' independent tasks that need to be executed across these nodes. Each task 'i' requires a specific amount of processing power 'p_i' and generates a profit 'v_i' upon completion.  A node 'j' has a total processing capacity of 'c_j'.\n\nA task can only be assigned to a single node.  A node can execute multiple tasks, as long as the sum of processing power required by the assigned tasks does not exceed the node's capacity.\n\nThe goal is to maximize the total profit obtained by optimally assigning tasks to nodes.\n\nWrite a function that takes the node capacities, task processing power requirements, and task profits as input and returns the maximum achievable total profit.\n",
  "input_format": "The input consists of the following:\n\n*   `node_capacities`: A list of integers representing the processing capacity of each node.  `node_capacities[j]` represents the capacity of node 'j'.\n*   `task_processing_powers`: A list of integers representing the processing power required by each task. `task_processing_powers[i]` represents the processing power required by task 'i'.\n*   `task_profits`: A list of integers representing the profit generated by each task. `task_profits[i]` represents the profit generated by task 'i'.\n\nAll lists will have a length between 1 and 100. All elements in these lists will be non-negative integers.",
  "output_format": "An integer representing the maximum achievable total profit.",
  "constraints": "*   1 <= `n` (number of nodes) <= 100\n*   1 <= `m` (number of tasks) <= 100\n*   0 <= `node_capacities[j]` <= 1000\n*   0 <= `task_processing_powers[i]` <= 1000\n*   0 <= `task_profits[i]` <= 1000",
  "sample_input": "node_capacities = [10, 15, 20]\ntask_processing_powers = [5, 7, 12, 4, 9]\ntask_profits = [10, 15, 25, 8, 18]",
  "sample_output": "76",
  "difficulty": "hard"
}
```