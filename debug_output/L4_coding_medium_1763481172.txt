```json
{
  "title": "Log Aggregation and Rate Limiting",
  "description": "You are building a log aggregation system for a distributed application.  Each log entry has a timestamp (in seconds since the epoch) and an application ID. Your system needs to aggregate log entries from different applications within specific time windows and apply rate limiting to avoid overwhelming downstream services. \n\nGiven a stream of log entries, your task is to implement a function that aggregates the number of log entries for each application within a given time window and determines if any application has exceeded its rate limit during that window.  The aggregation should be based on sliding time windows.\n\nSpecifically, implement the `process_logs` function that takes the following inputs and performs the following steps:\n\n1. **Aggregate Log Counts:**  Maintain a record of the number of log entries for each application within a specified time window. The window is defined by `window_size` seconds.\n2. **Sliding Window:**  The window slides forward in time as new log entries arrive. Older log entries outside the window are automatically discarded. Use a data structure optimized for efficient removal of old entries.\n3. **Rate Limit Check:** For each application, determine if its log entry count within the current window exceeds a predefined rate limit. The rate limit is given by `rate_limit` (log entries per window).\n\nThe function should return a list of application IDs that have exceeded the rate limit within the current window.  This list should be sorted in ascending order.\n",
  "input_format": "The input consists of the following parameters:\n\n*   `logs`: A list of tuples. Each tuple represents a log entry and has the form `(timestamp, application_id)` where:\n    *   `timestamp` is an integer representing the timestamp of the log entry in seconds since the epoch.\n    *   `application_id` is an integer representing the ID of the application that generated the log entry.\n*   `window_size`: An integer representing the size of the time window in seconds.\n*   `rate_limit`: An integer representing the maximum number of log entries allowed per application within the time window.\n\nAll timestamps in the `logs` list will be non-decreasing.",
  "output_format": "A list of integers representing the application IDs that have exceeded the rate limit within the current window. The list should be sorted in ascending order.",
  "constraints": "*   1 <= `len(logs)` <= 100,000\n*   1 <= `timestamp` <= 1,000,000,000\n*   1 <= `application_id` <= 100\n*   1 <= `window_size` <= 60\n*   1 <= `rate_limit` <= 1000",
  "sample_input": "logs = [(1678886400, 1), (1678886401, 2), (1678886402, 1), (1678886403, 1), (1678886404, 2), (1678886459, 1), (1678886460, 1)]\nwindow_size = 5\nrate_limit = 2",
  "sample_output": "[1]",
  "difficulty": "medium"
}
```