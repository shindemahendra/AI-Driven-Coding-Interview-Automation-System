```json
{
  "title": "Log Aggregation and Time-Based Filtering",
  "description": "You are tasked with building a log aggregation system for a microservices architecture. The system receives log entries from various services. Each log entry contains a timestamp, service name, and a log message. Your goal is to efficiently filter and retrieve log entries within a specific time range, grouped by service.\n\nSpecifically, you need to implement a function that takes a list of log entries and a time range (start and end timestamps) as input. The function should return a dictionary where the keys are service names and the values are lists of log messages from that service within the specified time range. The log messages within each service's list should be ordered chronologically (based on the timestamp).",
  "input_format": "The input consists of:\n\n1.  `logs`: A list of tuples. Each tuple represents a log entry and contains the following:\n    *   `timestamp`: An integer representing the Unix timestamp (seconds since epoch).\n    *   `service_name`: A string representing the name of the service that generated the log.\n    *   `log_message`: A string representing the log message.\n\n2.  `start_time`: An integer representing the start timestamp (inclusive) of the time range.\n\n3.  `end_time`: An integer representing the end timestamp (inclusive) of the time range.",
  "output_format": "A dictionary where:\n\n*   Keys: Service names (strings).\n*   Values: A list of log messages (strings) from that service within the specified time range, ordered chronologically.",
  "constraints": "\n*   1 <= Number of log entries <= 10^5\n*   1 <= timestamp <= 10^9\n*   1 <= Length of service_name <= 20\n*   1 <= Length of log_message <= 100\n*   start_time <= end_time\n*   All timestamps are valid Unix timestamps (seconds since epoch).",
  "sample_input": "logs = [\n    (1678886400, 'auth-service', 'User authentication successful'),\n    (1678886460, 'order-service', 'Order created successfully'),\n    (1678886430, 'auth-service', 'Password reset request'),\n    (1678886520, 'order-service', 'Payment processing initiated'),\n    (1678886340, 'user-service', 'User profile updated'),\n    (1678886580, 'auth-service', 'Account locked due to multiple failed attempts')\n]\nstart_time = 1678886400\nend_time = 1678886550",
  "sample_output": "{\n    'auth-service': [\n        'User authentication successful',\n        'Password reset request'\n    ],\n    'order-service': [\n        'Order created successfully',\n        'Payment processing initiated'\n    ]\n}",
  "difficulty": "medium"
}
```