```json
{
  "title": "Log Aggregation and Anomaly Detection",
  "description": "You are working on a log aggregation system for a large distributed application. The logs are streamed from various servers and contain information about requests, errors, and performance metrics. Your task is to process these logs in real-time and detect potential anomalies based on a rolling average of request durations.\n\nEach log entry contains a timestamp, server ID, request ID, and request duration (in milliseconds). You need to implement a function that takes a stream of log entries and calculates the rolling average request duration for each server over a fixed window size. If the current request duration for a server significantly deviates from its rolling average (more than a given threshold), flag it as a potential anomaly. The rolling average is computed using the *previous* `window_size` log entries for that specific server. If a server has fewer than `window_size` previous log entries, its rolling average is calculated using all available logs for that server.\n\nSpecifically, your function should process a list of log entries, calculate the rolling average for each server after each entry, and return a list of boolean values, where each value indicates whether the corresponding log entry is considered an anomaly. An entry is an anomaly if the absolute difference between its duration and the server's rolling average at that point exceeds the specified threshold.  If there are no previous logs for that server, the rolling average is considered to be 0.",
  "input_format": "The input is a list of tuples, where each tuple represents a log entry. Each tuple contains the following elements:\n\n*   `timestamp` (int): The timestamp of the log entry.\n*   `server_id` (str): The ID of the server that generated the log entry.\n*   `request_id` (str): The ID of the request.\n*   `duration` (int): The duration of the request in milliseconds.\n\nYour function should also accept two additional parameters:\n\n*   `window_size` (int): The size of the rolling window for calculating the average.\n*   `threshold` (int): The threshold for anomaly detection.",
  "output_format": "The output is a list of boolean values, where each value corresponds to a log entry in the input list. Each boolean value indicates whether the corresponding log entry is considered an anomaly (True) or not (False).",
  "constraints": "*   1 <= Number of log entries <= 10^5\n*   1 <= `timestamp` <= 10^9\n*   1 <= length of `server_id` <= 10\n*   1 <= length of `request_id` <= 10\n*   1 <= `duration` <= 10^4\n*   1 <= `window_size` <= 50\n*   1 <= `threshold` <= 10^3",
  "sample_input": "log_entries = [\n    (1678886400, 'serverA', 'req1', 100),\n    (1678886401, 'serverB', 'req2', 150),\n    (1678886402, 'serverA', 'req3', 120),\n    (1678886403, 'serverA', 'req4', 500),\n    (1678886404, 'serverB', 'req5', 180),\n    (1678886405, 'serverA', 'req6', 110)\n]\nwindow_size = 3\nthreshold = 200",
  "sample_output": "[False, False, False, True, False, False]",
  "difficulty": "medium"
}
```