```json
{
  "title": "Optimal Network Partitioning",
  "description": "A large cloud provider uses a graph-based data structure to represent its network infrastructure.  Nodes in the graph represent servers, and edges represent network connections between servers.  Each server (node) has a 'cost' associated with running it, represented as an integer. The company needs to partition its network into two distinct, non-overlapping subnetworks A and B to isolate a new service. The goal is to minimize the total operational cost of the *more expensive* of the two subnetworks. The cost of a subnetwork is simply the sum of the costs of the servers (nodes) it contains. You are given the number of nodes 'n', the list of edges representing the network connections, and an array 'costs' representing the cost of each node. Find the minimum possible cost of the more expensive subnetwork after partitioning the network. The network must be partitioned in such a way that no servers in subnetwork A are connected to servers in subnetwork B (i.e., subnetworks must be independent).",
  "input_format": "The first line contains an integer 'n', the number of servers in the network (nodes). The second line contains a list of edges represented as pairs of integers, where each pair (u, v) indicates an undirected edge between servers u and v (0-indexed). The list is terminated by a pair (-1, -1). The third line contains a list of integers representing the cost of each server, where costs[i] is the cost of server i (0-indexed).",
  "output_format": "Output a single integer representing the minimum cost of the more expensive subnetwork after partitioning the network.",
  "constraints": "1 <= n <= 1000\n0 <= u, v < n\n-1000 <= costs[i] <= 1000\nThere will be no duplicate edges.\nThe graph may not be fully connected (may have multiple connected components).",
  "sample_input": "5\n0 1\n0 2\n1 3\n-1 -1\n1 2 3 4 5",
  "sample_output": "6",
  "difficulty": "hard"
}
```