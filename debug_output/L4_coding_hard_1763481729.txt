```json
{
  "title": "Optimal Resource Allocation in a Distributed System",
  "description": "You are managing a distributed computing system with `n` nodes. Each node `i` has a limited resource capacity `capacity[i]`.  You have `m` independent tasks to execute. Each task `j` requires a certain amount of resources `resource_needed[j]` and generates a profit `profit[j]` upon completion. A task can only be executed on a single node, but a single node can execute multiple tasks as long as its resource capacity isn't exceeded.  Your goal is to maximize the total profit by optimally assigning tasks to nodes. \n\nDevelop an algorithm that determines the optimal assignment of tasks to nodes to maximize the total profit. Note that it's not mandatory to execute all tasks.",
  "input_format": "The input consists of the following:\n\n*   `n`: An integer representing the number of nodes.\n*   `capacity`: An array of `n` integers where `capacity[i]` represents the resource capacity of the i-th node.\n*   `m`: An integer representing the number of tasks.\n*   `resource_needed`: An array of `m` integers where `resource_needed[j]` represents the resource requirement of the j-th task.\n*   `profit`: An array of `m` integers where `profit[j]` represents the profit generated by the j-th task.",
  "output_format": "Return a single integer representing the maximum possible profit that can be achieved by optimally assigning tasks to nodes.",
  "constraints": "*   1 <= n <= 15\n*   1 <= m <= 200\n*   1 <= capacity[i] <= 500 for all i\n*   1 <= resource_needed[j] <= 500 for all j\n*   1 <= profit[j] <= 1000 for all j",
  "sample_input": "3\n[10, 15, 20]\n5\n[5, 7, 3, 8, 2]\n[10, 15, 6, 18, 4]",
  "sample_output": "53",
  "difficulty": "hard"
}
```