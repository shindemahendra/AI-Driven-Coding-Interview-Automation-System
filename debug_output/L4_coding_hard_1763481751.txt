```json
{
  "title": "Optimizing Cloud Resource Allocation for Peak Loads",
  "description": "A cloud service provider needs to optimize resource allocation across multiple data centers to handle fluctuating demand from various applications. Each application experiences a 'peak load' at a specific time of day. The goal is to minimize the total cost of running the servers while ensuring all applications have sufficient resources to handle their peak loads. \n\nYou are given the following information:\n\n*   `applications`: A list of applications. Each application `i` has a `peak_load[i]` representing the number of server cores required to handle its peak load, and `location[i]` specifying the data center where the application is running. Location is represented by an integer ID.\n*   `datacenters`: A list of data centers. Each datacenter `j` has a `capacity[j]` representing the total number of server cores available, and `cost_per_core[j]` representing the cost to run one server core for a single unit of time (e.g., one hour).\n\nYour task is to determine the minimum total cost to run the necessary server cores to meet the peak load demands of all applications.  You cannot move applications between data centers. You must allocate enough cores at each datacenter to cover the combined peak load of all applications running in that datacenter.  If a data center does not have sufficient capacity to meet the combined peak load of all applications in that datacenter, return -1 to signify that the problem is unsolvable.",
  "input_format": "The input will be provided as follows:\n\n*   `applications`: A list of dictionaries. Each dictionary represents an application and has the following keys:\n    *   `peak_load`: An integer representing the number of server cores required.\n    *   `location`: An integer representing the data center ID where the application is located.\n*   `datacenters`: A list of dictionaries. Each dictionary represents a data center and has the following keys:\n    *   `capacity`: An integer representing the total number of server cores available.\n    *   `cost_per_core`: An integer representing the cost to run one server core.\n",
  "output_format": "Return an integer representing the minimum total cost to run the servers. If it's not possible to allocate resources to satisfy the peak load demand in at least one datacenter, return -1.",
  "constraints": "*   1 <= Number of applications <= 100\n*   1 <= Number of data centers <= 10\n*   1 <= `peak_load` of each application <= 50\n*   1 <= `capacity` of each data center <= 1000\n*   1 <= `cost_per_core` of each data center <= 100\n*   Data center IDs are consecutive integers starting from 0.\n*   `location` of each application is a valid data center ID.",
  "sample_input": "{\n  \"applications\": [\n    {\"peak_load\": 20, \"location\": 0},\n    {\"peak_load\": 30, \"location\": 0},\n    {\"peak_load\": 15, \"location\": 1},\n    {\"peak_load\": 25, \"location\": 1},\n    {\"peak_load\": 10, \"location\": 2}\n  ],\n  \"datacenters\": [\n    {\"capacity\": 60, \"cost_per_core\": 5},\n    {\"capacity\": 45, \"cost_per_core\": 10},\n    {\"capacity\": 15, \"cost_per_core\": 20}\n  ]\n}",
  "sample_output": "1250",
  "difficulty": "hard"
}
```