```json
{
  "title": "Server Log Analysis: Identifying Peak Load Periods",
  "description": "You are a system administrator responsible for monitoring a web server's performance. You have access to the server's access logs, which record the number of requests received per minute. Your task is to identify the time intervals where the server experienced the highest request load. Given a list of minute-by-minute request counts, find the `k` consecutive minutes with the highest total request count.",
  "input_format": "The input consists of two lines.\n\nThe first line contains two space-separated integers, `n` and `k`, where `n` is the total number of minutes in the log, and `k` is the length of the time interval to consider.\n\nThe second line contains `n` space-separated integers, representing the number of requests received in each minute. These integers are the request counts for minutes 1 through `n`.",
  "output_format": "Output a single integer representing the starting minute of the `k` consecutive minute interval with the highest total request count. If there are multiple intervals with the same highest total, output the earliest starting minute. Minutes are numbered from 1 to n.",
  "constraints": "2 <= n <= 10^5\n1 <= k <= n\n0 <= request_count <= 10^4 for each minute.",
  "sample_input": "10 3\n1 4 2 5 1 3 2 4 1 2",
  "sample_output": "4",
  "difficulty": "medium"
}
```