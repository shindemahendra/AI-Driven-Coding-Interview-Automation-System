```json
{
  "title": "Optimal Resource Allocation for Cloud Services",
  "description": "A cloud provider offers various services (e.g., compute, storage, database) to its clients. Each service has a cost and resource consumption profile. A client has a specific budget and resource limits. The goal is to maximize the client's utilization of services while staying within their budget and resource limits.\n\nYou are given the following information:\n\n*   `budget`: The client's total budget (an integer).\n*   `resource_limits`: A dictionary representing the client's resource limits.  The keys are resource types (e.g., 'cpu', 'memory', 'disk') and the values are the maximum allowed usage for each resource type (integers).\n*   `services`: A list of dictionaries, where each dictionary represents a service. Each service dictionary has the following keys:\n    *   `name`: The name of the service (string).\n    *   `cost`: The cost of the service (an integer).\n    *   `resource_consumption`: A dictionary representing the resource consumption of the service. The keys are resource types (e.g., 'cpu', 'memory', 'disk') and the values are the resource usage of the service (integers).\n\nDetermine the maximum number of distinct services the client can utilize without exceeding their budget or resource limits. You can use each service at most once. Consider all possible combinations of services to find the optimal set.\n\n**Example:**\nIf the client has a budget of 100, CPU limit of 10, and two services:\n*   Service A: cost = 60, CPU = 5\n*   Service B: cost = 40, CPU = 4\n\nThe optimal solution is to use both services (A and B), utilizing a total budget of 100 and CPU of 9. The maximum number of distinct services is 2.",
  "input_format": "The input will be passed as function arguments:\n\n*   `budget`: An integer representing the client's budget.\n*   `resource_limits`: A dictionary where keys are resource types (strings) and values are the maximum resource limits (integers).\n*   `services`: A list of dictionaries, where each dictionary represents a service. Each service dictionary contains 'name' (string), 'cost' (integer), and 'resource_consumption' (dictionary with resource types as keys and resource consumption as values (integers)).",
  "output_format": "An integer representing the maximum number of distinct services the client can utilize without exceeding their budget or resource limits.",
  "constraints": "*   1 <= budget <= 1000\n*   1 <= number of services <= 20\n*   1 <= cost of each service <= 500\n*   1 <= resource limit of each resource type <= 100\n*   0 <= resource consumption of each service <= 50 for each resource type",
  "sample_input": "budget = 100\nresource_limits = {'cpu': 10, 'memory': 8}\nservices = [\n    {'name': 'A', 'cost': 60, 'resource_consumption': {'cpu': 5, 'memory': 3}},\n    {'name': 'B', 'cost': 40, 'resource_consumption': {'cpu': 4, 'memory': 5}},\n    {'name': 'C', 'cost': 30, 'resource_consumption': {'cpu': 2, 'memory': 2}}\n]",
  "sample_output": "3",
  "difficulty": "hard"
}
```